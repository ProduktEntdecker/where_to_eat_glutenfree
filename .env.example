# Copy this file to .env and add your free API keys

# ⚠️ IMPORTANT: These APIs require a backend proxy for security!
# DO NOT use VITE_ prefix - that exposes keys to the client
# These should only be used in your backend/server code

# Backend-only: Foursquare Places API (Free: 100,000 calls/month)
# Sign up at: https://foursquare.com/developers/
# Use in backend proxy only - never expose to client
FOURSQUARE_API_KEY=

# Backend-only: Yelp Fusion API (Free: 5,000 calls/day)
# Sign up at: https://www.yelp.com/developers
# Use in backend proxy only - never expose to client
YELP_API_KEY=

# Note: OpenStreetMap doesn't need an API key - it just works!
