import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.produktentdecker.gffinder',
  appName: 'GF Finder',
  webDir: 'dist'
};

export default config;
